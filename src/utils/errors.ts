import { t } from "i18next";

export const ERRORS = {
  SESSION_EXPIRED: t("Session Expired").toString(),
  USER_NOT_FOUND: t("User not found").toString(),
  INVALID_PASSWORD: t("Invalid password").toString(),
  WRONG_OTP: t("Wrong OTP! Please retry.").toString(),
  USER_INACTIVE: t(
    "Your account is deactivated please contact to Super admin"
  ).toString(),
};
